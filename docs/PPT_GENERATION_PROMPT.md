# PPT 生成提示词（可直接复制给其他 AI）

> 用途：把下面整个「提示词」丢给 AI 生成器（GPT/Gamma/豆包/AIPPT 等），即可得到一份内容完整、术语准确的比赛演示 PPT。
> 前提：提示词里的事实均来自当前真实制品（已部署网站、代码库、测试、文档），可直接照抄。

---

## —— 从这里开始复制 ——

你是一名马来西亚 SME 数字化/财税领域的资深产品与商业演示设计师。请为「LedgerLens AI」生成一份 16:9、中文、约 20 页的科技感演示 PPT（深蓝底 NAVY `#0b1f3a` + 品牌蓝 `#3667f5` + 浅底 `#f5f7fa`，白底与浅色底交替，卡片式布局，每页一句主标题、副标题、要点与演讲备注）。

### 你的任务是：以"真实、可验证、不夸大"为底线，完整呈现这款"收据→财务闭环"AI 产品。

**必须严格遵守的事实与边界（不允许夸大成已上线/已接通）：**
- 产品一句话定位：**员工提交，AI 理解，管理员负责，账目可追溯。**
- 面向：马来西亚小微企业（SME）；背景：MyInvois/LHDN 电子发票规范化、收据管理杂乱、财务人手不足。
- 当前是**比赛原型**：WhatsApp、MyInvois/LHDN 均为**模拟/演示接入**，未发起真实 Meta/LHDN API 调用；在每处提及必须标注「模拟接入」「生产路线图」。
- 报表为**管理层报表草稿**，正式 MPERS/MFRS 报表须会计师审核；AI 无权绕过管理员入账。
- 比赛版 Gemini Key 由浏览器本地输入，仅用于本地演示；生产版需后端代理 + Secret Manager + 加密存储。
- 已有**在线演示**：https://ledgerlens-ai-p819.onrender.com （免费实例，首次访问可能需唤醒 30–60 秒）。
- 代码仓库：https://github.com/marcuslim318-cloud/project-test （MIT License）。
- 可复现：`npm test`，共 **7 个集成测试**（登录安全、正常收据、重复收据、低置信度/缺 MyInvois、WhatsApp 模拟入队、凭证哈希存证、管理员数据重置）。
- 本地运行：`node server.mjs` → http://localhost:8000。

**演示账号：**
- 管理员 `admin / admin123`（审核、入账、报表、AI/WhatsApp 设置）
- 财务管理员 `finance / fin123`
- 员工 `employee / emp123`、员工2 `staff2 / staff123`

**请务必包含以下事实页：**

1. **封面**：品牌名 LedgerLens AI｜副标题「把一张收据，变成一套可追溯的财务决策」｜「AI 财务闭环原型 · 面向马来西亚小微企业」。
2. **背景与痛点**：MyInvois 强制电子发票趋势下，SME 员工收据乱、财务人手少、老板看不到实时经营数据、重复报销难防。
3. **产品解**：上传/WhatsApp 收图 → AI 提取字段 → 规则引擎风险判定 → 管理员审核 → 复式分录 → 报表与审计轨迹 → 通知闭环。强调这是「闭环」不是「OCR」。
4. **现场演示流程**：员工登录上传演示收据 → 管理员登录审核中心（列出 `/api/receipts` 全部待审核单据，点击查看）→ 确认入账 → 报表更新 → 员工收到通知 → 被拒单据在「我的收据」页查看原因并重新提交。
5. **核心功能清单**：角色权限（admin/finance/employee）、AI 收据识别、后端规则引擎（重复收据/金额税额校验/低置信度/缺失 MyInvois 自动标记待复核）、共享审核队列、凭证 SHA-256 存证、状态通知、管理报表、CSV 导出、移动端可用、三种演示数据（正常/低置信度/重复）、管理员数据管理与清空。
6. **后端规则引擎（确定性、可复现）**：四类判定规则 + 输出 risk/riskReason/checks。
7. **技术架构**：Web UI → Node.js HTTP API → data.json；Token/RBAC；Gemini（可选本地演示）；MyInvois/WhatsApp 为生产路线。比赛版全局架构图。
8. **安全性**：登录 API 绝不返回密码；凭证哈希存证；权限隔离；比赛版边界（明文口令/单文件库仅演示）。
9. **可复现测试**：正常收据闭环；重复收据；模糊/缺失 MyInvois 高风险单据；WhatsApp 模拟入队；凭证哈希存证；管理员数据重置（共 7 条，`npm test` 全绿）。
10. **用户价值与成本**：减少手工录入、拦截重复报销、审核周期缩短、收据→报表可追溯；对比「原来对账 X 分钟 → 现在几分钟」的量化话术。
11. **商业模式**：Starter RM49/月、Growth RM149/月、Pro RM299/月起、设置费 RM300–800（建议价，非已验证报价）。
12. **集成路线（诚实）**：WhatsApp → Meta Cloud API Webhook；MyInvois → LHDN API TIN/UUID/状态；数据库 → PostgreSQL/Supabase；AI Key → 后端代理；对象存储加密原件。
13. **90 天落地计划**：0–30 天试点 MVP（换 Supabase/后端代理/审计日志/3–5 家 SME 试点）→ 31–60 天合规与协作（MyInvois 沙盒、科目表预算、会计师复核）→ 61–90 天多入口与商业化（WhatsApp Cloud API、SaaS 计费、监控备份）。附成功指标：字段准确率、人工审核率、收据到入账时间、重复报销拦截数、活跃企业数。
14. **已知边界/自我澄清（答辩用）**：是否真财报？MyInvois 接通了吗？AI 识别错金额怎么办？Key/数据安全？为什么不用完整 ERP？——给出诚实但不掉分的回答。
15. **评审 Q&A**：5 组常见质疑及答案（同上原则）。
16. **交付物清单**：项目介绍、技术方案、代码仓库、Demo 视频脚本（docs/DEMO_VIDEO_SCRIPT.md）、开源协议（MIT）、落地计划、（在线部署地址）。可运行·可验证·可复现定位。
17. **互动页（推荐）**：「请用浏览器打开 https://ledgerlens-ai-p819.onrender.com ，用 admin/admin123 登录体验」+ GitHub 二维码。
18. **结尾**：一句话定位收尾 + 联系/仓库信息。

**设计规范：**
- 每页含：页眉小字标签（如 01 · 背景与痛点）、主标题、副标题、要点、底部页脚 "LEDGERLENS AI · MALAYSIA SME FINANCE · 页码"。
- 演讲备注：每页给 2–3 句口播提示，尤其强调「模拟接入，未夸大」。
- 配色克制、信息不堆砌；允许一页一图/一流程；避免花哨动效描述；中英混排即可。

## —— 从这里结束复制 ——

---

## 给 AI 生图/加图的可选附加指令

如需让 AI 顺手生成配图，可追加到提示词末尾：
> 为第 3、5、9、12 页生成简洁的示意插图：收据→审核→报表闭环流程图、角色权限图、风险判定流程图、WhatsApp/MyInvois 集成路线图。风格扁平、品牌蓝为主、不要出现真实企业的敏感信息。